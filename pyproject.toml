[project]
name = "stock_crm16"
authors = [
    { name = "Off Market Venture", email = "admin@offmarketventure.com"}
]
description = "Share Market & Unlisted Equity CRM for Frappe Framework & ERPNext v16"
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.bench.dev-dependencies]

[deploy.dependencies.apt]
packages = []
